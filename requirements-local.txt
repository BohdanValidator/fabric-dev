# Spark + Delta — must match your Fabric runtime versions
pyspark==3.5.1
delta-spark==3.1.0

# Notebook kernel + display() shim
ipykernel==6.29.0
ipython==8.26.0
pandas==2.2.3
numpy==1.26.4
itables==2.2.4

# Local config
python-dotenv==1.0.1

# OneLake file access (Excel configs)
adlfs
azure-identity
openpyxl==3.1.2